#!/bin/sh
#

#
# execute php sync client
#
cmd=${1:-help}
pid=$$
dir=`dirname $0`

cd $dir ;

if [ "$cmd" = "sync" ]; then

    echo `date +%s` > /tmp/sync_client.request

else
    exec ${dir}/cake/console/cake -app ${dir}/app sync_client ${cmd}
fi
